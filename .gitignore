# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (never commit real values) — .env.example is tracked, see below
.env
.env*.local
!.env.example

# vercel
.vercel/

# Supabase CLI's own disposable cache (not the migrations we author by hand)
supabase/.temp/

# typescript build info
*.tsbuildinfo

# OS / editor cruft
.DS_Store
*.pem
.vscode/

# next.js generated
next-env.d.ts
/coverage

# local-only — planning docs, project instructions, local Claude config (never pushed)
/.claude/
/docs/
/CLAUDE.md
